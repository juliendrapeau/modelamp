[project]
name = "modelamp"
version = "0.1.0"
description = "="
authors = [{ name="Julien Drapeau", email="juliendrapeau707@gmail.com" }]
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["modelamp"]